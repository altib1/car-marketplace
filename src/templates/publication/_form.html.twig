{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn mt-5 bg-green-500 hover:bg-green-600 text-white py-2 px-4 rounded-md shadow-md transition-all">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
